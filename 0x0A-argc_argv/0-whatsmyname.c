#include <stdio.h>

/**
 * main - print its name
 * @argc: num or arguments
 * @argv: array of arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
